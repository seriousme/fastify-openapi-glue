export default data => `// Fastify plugin autogenerated by fastify-openapi-glue
import openapiGlue from "${data.pluginPackageName}";
import { dirname } from "${data.dirnameModule}";
const dir = dirname(import.meta);

const options = {
  specification: \`\${dir}/${data.specFile}\`,
  service: \`\${dir}/${data.service}\`,
  securityHandlers: \`\${dir}/${data.security}\`
};

export default async function (fastify, opts) {
  fastify.register(openapiGlue, options);
};
`;
